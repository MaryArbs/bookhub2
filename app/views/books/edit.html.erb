<h1> Edit your book!</h1>

<%= render 'layouts/errors', {object: @book} %>


<%= form_with(model: @book, local:true) do |f| %>
  
  <%= render 'form',  {f: f} %> 
    
<%= f.submit "Create" %>
<% end %>


